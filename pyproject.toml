[project]
name = "asr-bot"
version = "0.1.0"
authors = ["Quadrobebras"]
description = ""

dependencies = [
    "fastapi",
    "uvicorn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
